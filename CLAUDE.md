
## The Claude Hermes Link

When Qadir says **"Use the Claude Hermes Link"**, use the existing private GitHub
repository `aeeg-office/ai-coordination` and follow its current `PROTOCOL.md` and
`CLAUDE_HERMES_LINK.md` (do not rely on stale copied instructions — pull latest `main`).
Join an existing ACTIVE/REVIEW task for this project/objective, or create one via
`scripts/task_new.sh`. Exchange work with Hermes through GitHub append-only messages,
inspect each other's referenced commits, and continue until the task closes.
Do **not** ask Qadir to manually transfer messages between Claude and Hermes.
`ai-coordination` is the communication/task layer only; application source stays here.
